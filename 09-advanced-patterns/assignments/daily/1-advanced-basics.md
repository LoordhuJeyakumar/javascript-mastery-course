/**
 * # Daily Practice - Advanced Patterns
 * 
 * Practice these concepts to build muscle memory. Don't look at the solutions!
 */

/**
 * ## 🔍 Task 1: Phone Number Validator (Regex)
 * Create a function `isValidPhoneNumber(phone)` that returns `true` if a string matches the format `XXX-XXX-XXXX`.
 * 
 * **Problem Statement:**
 * - Use a Regular Expression pattern.
 * - Input: "123-456-7890" -> Output: true
 * - Input: "12-34-56" -> Output: false
 */

/**
 * ## ⚡ Task 2: Safety First (Optional Chaining)
 * You are building a weather app. The API response might be missing the `wind` property.
 * 
 * **Problem Statement:**
 * - Access `response.current.weather.wind.speed` safely.
 * - If it exists, return the speed.
 * - If it doesn't, return `undefined` (The app should NOT crash).
 */

/**
 * ## 📦 Task 3: The Default Value (Nullish Coalescing)
 * Create a function `getVolume(level)` that returns the volume.
 * 
 * **Problem Statement:**
 * - If `level` is `null` or `undefined`, default to `50`.
 * - If `level` is `0`, it should return `0` (Zero is a valid volume!).
 */

/**
 * ## 📅 Task 4: Future Date (Date Object)
 * Write a function that returns a Date object exactly 7 days from today.
 * 
 * **Problem Statement:**
 * - Use the `new Date()` object.
 * - Hint: There are `1000 * 60 * 60 * 24` milliseconds in a day.
 */
