// callbacks.js
// 🎯 Assignment: Callback Power

/*
  PROBLEM 1: Simple Runner
  Create a function 'runTask' that takes a task name and a callback function.
  It should log "Starting {taskName}" and then call the callback.
*/

// Hint: Follow the pattern -> function(arg, callback) { ... callback(); }


/*
  PROBLEM 2: Mathematical Callback
  Create a function 'operate(a, b, callback)' that executes the callback on numbers 'a' and 'b'.
  Use it to subtract two numbers.
*/

// Hint: Pass an anonymous function as the third argument when calling 'operate'.


/*
  PROBLEM 3: Delayed Welcome
  Create a function 'welcomeUser(name)' that logs "Welcome, {name}" after 2 seconds.
  Use 'setTimeout' which uses a callback.
*/

// Hint: setTimeout(callback, milliseconds);
