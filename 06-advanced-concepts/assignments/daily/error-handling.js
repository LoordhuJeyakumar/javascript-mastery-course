// Assignment: Error Handling

// 1. Create a function `safeParse(jsonString)` that attempts to parse a string.
// If it fails, catch the error and return a custom message like "Invalid JSON provided".
// Always log "Parsing attempt finished" in the finally block.

function safeParse(jsonString) {
    // Your code here:
}

// Test cases:
// console.log(safeParse('{"age": 30}'));
// console.log(safeParse('not a json'));
