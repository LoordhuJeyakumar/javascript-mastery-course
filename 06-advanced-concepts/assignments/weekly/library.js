// Weekly Challenge: Library System Pro

// 1. Create a class `Book` with properties: `title`, `author`, `isBorrowed` (default false).
class Book {
    // Your code here
}

// 2. Create a class `Library`.
// It should have an array `books`.
// Add a method `addBook(book)` to add a book to the library.
// Add a method `borrowBook(title)`:
//    - Find the book by title.
//    - If NOT found, THROW AN ERROR: "Book not found".
//    - If book is already borrowed, THROW AN ERROR: "Book is already out".
//    - Otherwise, mark `isBorrowed = true`.

class Library {
    // Your code here
}

// 3. TEST SYSTEM
// Wrap your testing code in a try...catch block to handle the errors gracefully!
// Example:
// try {
//    myLibrary.borrowBook("Harry Potter");
// } catch (err) {
//    console.error("Library Error:", err.message);
// }
